# Default values for xldeploy-argos-collector.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

argos-collector:
  collector:
    type: XLDEPLOY
    baseurl: ""
  ingress:
    hosts:
    - host: xldeploy-argos-collector.local
      paths:
      - /
